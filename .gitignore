.Rproj.user
.Rhistory
.RData
.Ruserdata

/.quarto/
**/*.quarto_ipynb

# Confidential source material for the Data Collection project.
# Real company data (stock, sales orders, customers, finance) + code with
# internal file paths. Kept private on disk, NEVER published to GitHub.
# The public write-up lives in posts/data_collection_jbs/ instead.
/posts/Data collection project/

# Confidential source material for the Shiny dashboard project.
# App code + .RData/.Rhistory that may hold real operational data.
# Kept private on disk; the public write-up lives in posts/shiny_app_jbs/ instead.
/posts/Interactive dashboard app/

# Confidential source material for the Batch Consolidation project.
# Real stock data (Database/) + code and .RData/.Rhistory that may hold it.
# Kept private on disk; the public write-up lives in posts/data_consolidation_jbs/ instead.
/posts/Data Consolidation/
